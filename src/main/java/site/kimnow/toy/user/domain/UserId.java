package site.kimnow.toy.user.domain;

import site.kimnow.toy.common.base.domain.LongTypeIdentifier;

public class UserId extends LongTypeIdentifier {
    public UserId(Long id) {
        super(id);
    }
}
