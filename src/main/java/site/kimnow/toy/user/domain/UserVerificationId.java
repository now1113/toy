package site.kimnow.toy.user.domain;

import site.kimnow.toy.common.base.domain.LongTypeIdentifier;

public class UserVerificationId extends LongTypeIdentifier {
    public UserVerificationId(Long id) {
        super(id);
    }
}
